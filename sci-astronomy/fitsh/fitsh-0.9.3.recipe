SUMMARY="Utilities related to astronomical data reduction and processing"
DESCRIPTION="The FITSH software package provides a set of independent
binary programs (called Tasks) that are designed for execution from a
UNIX command line shell or shell script.
Each of these tasks performs a specific operation that performs a
single step related to astronomical data reduction and image processing."
HOMEPAGE="https://fitsh.net/"
COPYRIGHT="2011 - 2019, András Pál,"
LICENSE="GNU GPL v3"
REVISION="1"
SOURCE_URI="https://fitsh.net/download/fitsh/fitsh-$portVersion.tar.gz"
CHECKSUM_SHA256="705987e8cc7514cca9a19ffc32d5a50e6f3a4a7421cc343b16ebb0c5df094efb"
ARCHITECTURES="!x86_gcc2 x86_64 ?arm ?ppc ?sparc"
SECONDARY_ARCHITECTURES="x86"

PROVIDES="
	fitsh${secondaryArchSuffix} = $portVersion
	"
REQUIRES="
	haiku${secondaryArchSuffix}
	#lib:libcfitsio${secondaryArchSuffix} >= 3.47
	"

BUILD_REQUIRES="
	haiku${secondaryArchSuffix}_devel
	#devel:libcfitsio${secondaryArchSuffix} >= 3.47
	"
BUILD_PREREQUIRES="
	cmd:aclocal
	cmd:autoreconf
	cmd:awk
	cmd:gcc${secondaryArchSuffix}
	cmd:make
	"

BUILD()
{
	if [ $effectiveTargetArchitecture == x86_64 ]; then
		GCC5FLAGS="--enable-x86-64"
	fi

	autoreconf -vfi
	runConfigure ./configure

	make $jobArgs
}

INSTALL()
{
	make install
}